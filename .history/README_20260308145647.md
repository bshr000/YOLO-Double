# YOLO-Double

基于 PyTorch 的双模态目标检测项目（Visible + Infrared），旨在完整打通多模态目标检测框架的基本流程。
整体思路参考YOLO-Single。

## 1. 项目简介

本项目面向 YOLO 格式标注（`class cx cy w h`，归一化坐标），核心特点：

- 双分支骨干网络（Visible 分支 + Infrared 分支）
- 多尺度特征融合（PAN-FPN）
- DFL 回归分支与 Anchor-Free 风格预测
- 训练支持 EMA、AMP、梯度裁剪
- 提供训练、验证、推理三条主流程脚本

## 2. 仓库结构

- `train.py`：模型训练入口
- `val.py`：验证评估（mAP@0.5 与 mAP@0.5:0.95）
- `inference.py`：离线推理与可视化保存
- `dataset.py`：双模态数据读取与增强
- `model_dual.py`：双分支检测模型定义
- `loss.py`：检测损失定义（CIoU + DFL + SimOTA）
- `utils.py`：工具函数（EMA、NMS、checkpoint）
- `config.yaml`：训练与数据配置

## 3. 环境安装

建议 Python 3.10。

```bash
pip install -r requirements.txt
```

## 4. 数据准备

默认代码通过路径替换规则进行双模态配对：`visible -> infrared`。  
也就是说，可见光图片路径中需要包含 `visible` 目录名，并且红外图片与其同名。

示例目录结构：

```text
your_dataset/
  images/
    visible/
      000001.png
      000002.png
    infrared/
      000001.png
      000002.png
  labels/
    visible/
      000001.txt
      000002.txt
  train.txt
  val.txt
```

其中：

- `train.txt` / `val.txt` 内容为 **visible 图像路径列表**
- 标签使用 YOLO 格式：`class cx cy w h`

## 5. 配置说明

在 `config.yaml` 中修改以下关键字段：

- `data.train_images`
- `data.train_labels`
- `data.val_images`
- `data.val_labels`
- `data.num_classes`

## 6. 使用方式

### 6.1 训练

```bash
python train.py --config config.yaml
```

### 6.2 验证（mAP）

```bash
python val.py --verbose --config config.yaml --checkpoint checkpoints/best_model.pth
```

### 6.3 推理

```bash
python inference.py --save-txt --source path/to/images_or_txt --config config.yaml --checkpoint checkpoints/best_model.pth --output inference_output
```

## 7. 结果展示，以YOLO-Double (CSPDarknet-S) 模型 vedai8数据集 100epoch 的权重为例

### 7.1   val

| Class | Precision | Recall | mAP@0.5 | mAP@0.5:0.95 |
| --- | --- | --- | --- | --- |
| all | 0.640 | 0.269 | 0.423 | 0.248 |
| 0 | 0.758 | 0.724 | 0.718 | 0.414 |
| 1 | 0.723 | 0.632 | 0.649 | 0.415 |
| 2 | 0.571 | 0.512 | 0.428 | 0.271 |
| 3 | 0.345 | 0.666 | 0.393 | 0.215 |
| 4 | 0.210 | 0.200 | 0.064 | 0.023 |
| 5 | 0.545 | 0.315 | 0.260 | 0.097 |
| 6 | 0.795 | 0.235 | 0.248 | 0.146 |
| 7 | 1.000 | 0.500 | 0.620 | 0.401 |

## 8. 致谢

部分实现思路参考 YOLO 系列开源项目（如 YOLOv5/YOLOv8）与相关社区实践。

